# FaceRecognition 人脸识别门禁系统 — 总体设计方案

> **作者**：向治昌  
> **最后更新**：2026-05-23

---

## 一、系统概述

### 1.1 系统定位

四端协同的人脸识别门禁系统：

```
手机（微信小程序）→ Windows 后台（FastAPI + Vue 3）→ 树莓派（推理）→ STM32（门锁控制）
   │                        │                          │               │
   人脸采集入口               用户管理 + 特征下发           摄像头+推理      UART 接收指令 + 继电器
```

- **手机端**：微信小程序，拍照 + 填姓名 → 上传到 Windows 后台
- **Windows 后台**：FastAPI REST API + Vue 3 管理界面 + SQLite + Pi SCP 同步
- **树莓派端**：USB 摄像头 + 人脸检测/识别推理 + UART 指令下发
- **STM32 端**：FreeRTOS + DMA UART 接收指令 + GPIO 控制继电器/门锁

### 1.2 核心工作流

```
正常识别流程:
  树莓派摄像头持续采集 → 检测到人脸 → 调用特征匹配
  → 匹配成功 → 通过 UART 发送 IDENTIFY 帧(0x10) 给 STM32
  → STM32 解析帧 → 驱动继电器开门 → 回复 ACK 帧
  → 匹配失败 → 发送 UNKNOWN 帧(0x11) → STM32 蜂鸣器告警

注册流程:
  管理员用小程序拍照 + 填姓名 → 上传到 Windows 后台
  → Windows 保存照片 + 提取人脸特征向量
  → Windows 通过 SCP 将特征文件传到树莓派 features/ 目录
  → 树莓派热加载新特征

心跳保活:
  树莓派每 5 秒发一次 HEARTBEAT 帧(0x1F) → STM32 回复 ACK
  → STM32 15 秒未收到心跳 → 判定 Pi 离线 → 打印告警
```

---

## 二、树莓派端（推理核心）

### 2.1 职责

1. **摄像头采集** — USB 摄像头实时画面采集
2. **人脸检测** — 检测画面中的人脸位置
3. **人脸识别** — 提取特征向量，与特征库匹配
4. **UART 通信** — 通过 UART 向 STM32 发送识别结果帧
5. **特征库管理** — 接收 Windows 下发的特征文件，热更新

### 2.2 推荐技术栈

| 层 | 选型 | 说明 |
|----|------|------|
| 人脸检测 | MTCNN 或 Ultralight(FastFaceNxN) | 轻量级检测模型 |
| 特征提取 | MobileFaceNet 或 ArcFace (100MB+) | 512 维特征向量 |
| 推理引擎 | ncnn / onnxruntime / Python | 当前 ncnn 有自定义层崩溃问题 |
| 特征库 | FeatureDB (磁盘文件) | ~/.faces/*.bin，内存索引 |
| 通信协议 | 自定义二进制帧 (USART) | 变长帧 + 转义 + XOR |

### 2.3 待完成

- 主程序线程调度 + 信号处理安全退出
- 解决 ncnn 自定义层导致推理崩溃的问题，或用 Python 版替代
- systemd 服务文件，实现开机自启
- 将特征注册接口通过网络暴露给 Windows 后台

---

## 三、STM32 端（通信与执行）

### 3.1 职责

1. **接收识别结果** — 从树莓派通过 UART (USART1) 接收人脸识别结果帧
2. **DMA+RingBuffer 收帧** — DMA1 循环模式自动收字节 + RingBuffer 缓冲 + IDLE 中断通知任务
3. **解析协议帧** — 状态机解码（7 状态），校验 XOR + 转义处理
4. **执行动作** — 根据识别结果执行相应的硬件操作

### 3.2 技术栈

| 层 | 选型 | 说明 |
|----|------|------|
| MCU | STM32F103C8T6 | Cortex-M3, 64KB SRAM, 128KB Flash |
| RTOS | FreeRTOS | 静态内存分配, NVIC_PriorityGroup_4 |
| 标准库 | STM32 FWlib 3.5 | SPL，不使用 HAL |
| UART 驱动 | DMA1 C5 + RingBuffer + IDLE 中断 | 高效中断安全架构 |
| 通信接口 | USART1 (PA9-TX, PA10-RX) | 115200-8-N-1 |
| 外设 | GPIO (PB5/PE5 LED), IWDG (看门狗) | — |
| 开发环境 | Keil MDK-ARM | .uvprojx 工程 |

### 3.3 UART 驱动架构（DMA+RingBuffer）

```
┌──────────────────────────────────────────────────────┐
│                  DMA1 循环模式                        │
│  USART1_RX 字节流 ──不可见──► DMA 自动填入 rx_buffer   │
│                    │                                  │
│           IDLE 中断（帧间隔检测）                       │
│                    │                                  │
│              清理 IDLE 标志 + 通知 RX 任务             │
│                    │                                  │
│              FreeRTOS 任务通知                         │
│                    │                                  │
│              vUartRxTask 被唤醒                       │
│                    │                                  │
│          读 RingBuffer 取出全部可用字节                │
│                    │                                  │
│           逐字节丢入状态机解析帧                        │
│                    │                                  │
│          dispatch_frame() 分发命令                    │
└──────────────────────────────────────────────────────┘
```

### 3.4 帧协议

```
┌──────┬──────┬──────┬─────────┬─────────┬──────────┬──────┬──────┐
│ 帧头  │ 命令  │ 方向  │ LEN_H   │ LEN_L   │ DATA     │ XOR  │ 帧尾  │
│ 1B   │ 1B   │ 1B   │ 1B      │ 1B      │ 变长     │ 1B   │ 1B   │
│ 0xAA │ CMD  │ DIR  │ (大端)   │         │ (转义)    │      │ 0x55 │
└──────┴──────┴──────┴─────────┴─────────┴──────────┴──────┴──────┘

方向 DIR: 0x01 = Pi/PC → STM32,  0x02 = STM32 → Pi/PC

转义 (仅 DATA 段):
  0xAA → 0xBB 0x55    0x55 → 0xBB 0xAA    0xBB → 0xBB 0x44

XOR = CMD ^ DIR ^ LEN_H ^ LEN_L ^ DATA[0] ^ ... ^ DATA[N-1]
(帧头 0xAA 和帧尾 0x55 不参与 XOR)
```

**Pi/PC → STM32 命令：**

| CMD | 常量 | 含义 | STM32 回复 |
|-----|------|------|-----------|
| 0x1F | HEARTBEAT | 心跳检测 (每 5 秒) | ACK(0x20) + "HB" |
| 0x10 | IDENTIFY | 识别成功(有人) | ACK(0x20) + 文本确认 |
| 0x11 | UNKNOWN | 有人脸未识别 | 文本告警 |
| 0x12 | NOFACE | 无人脸 | 文本提示 |
| 0x13 | MULTIFACE | 多人脸 | 文本告警 |

### 3.5 已完成 / 待完成

**✅ 已完成：**
- FreeRTOS 环境搭建（静态内存分配，stack overflow / malloc fail 钩子）
- DMA1 C5 循环模式 UART 接收 (USART1, 115200)
- RingBuffer 缓冲层（2 的幂大小，零拷贝）
- USART1 IDLE 中断 → FreeRTOS 任务通知
- 完整帧协议（7 状态状态机, 0xBB 转义, XOR 校验, 变长数据段）
- 5 条命令处理器 (IDENTIFY/UNKNOWN/NOFACE/MULTIFACE/HEARTBEAT)
- ACK 回复帧 (CMD_ACK + "HB" 数据)
- 15 秒心跳超时检测 + 告警 + 恢复自动清除
- 独立看门狗 (IWDG, 2s 超时, 500ms 喂狗)
- 双 LED 指示 (PB5 + PE5)
- PC 串口助手 HEX 模式全命令联调通过
- 三层诊断（T0 任务入口 + T1 创建验证 + T2 调度器确认）

**⚠️ 待完成：**
- 继电器 GPIO 控制及开门时序
- 蜂鸣器告警模式
- 树莓派 UART 硬件连接与全链路联调
- 按键输入（出门按钮/管理员键）
- OLED 显示

---

## 四、Windows 后台端

### 4.1 职责

1. **接收手机端上传的人脸照片** — 提供 HTTP API 接收图片
2. **人脸注册管理** — 存储用户信息（姓名、工号、人脸照片），维护人员数据库
3. **特征提取与下发** — 调用 Python 推理脚本提取人脸特征，将特征向量文件通过 SCP/SFTP 下发到树莓派
4. **管理界面** — Web 页面查看注册人员列表、识别日志等
5. **与树莓派通信** — 通过网络（SSH/自定义 TCP）向树莓派下发注册/删除指令

### 4.2 技术栈（已实现）

| 层 | 选型 | 说明 |
|----|------|------|
| 后端框架 | FastAPI（Python） | 轻量异步框架，自带 Swagger 文档 |
| 数据库 | SQLite + SQLAlchemy | 单文件，零配置 |
| 图像存储 | 本地文件系统 | 按用户 ID 分目录存放原始照片 |
| 前端 | Vue 3 + TypeScript + Element Plus | 仪表盘/人员管理/实时画面/系统配置 |
| 文件传输 | paramiko (SSH/SCP) | 将 .bin 特征文件传到树莓派 |
| 桌面窗口 | pywebview + Edge WebView2 | 原生窗口，无浏览器地址栏 |
| 打包 | PyInstaller | 单文件 EXE (~31MB) |

### 4.3 ✅ 已完成

- FastAPI REST API：注册/查询/删除/同步
- Vue 3 管理界面（4 页面：仪表盘/人员管理/实时画面/系统配置）
- SQLite 数据库
- Pi SSH/SCP 同步（单用户 + 批量）
- 手机端 `POST /api/register` 接口
- PyInstaller 打包为单文件 EXE

---

## 五、手机端（小程序）

### 5.1 职责

1. **人脸采集** — 调用手机摄像头拍照或从相册选取
2. **照片上传** — 将照片和用户填写的姓名通过 HTTP 上传到 Windows 后台

### 5.2 技术栈（已实现）

| 层 | 选型 | 说明 |
|----|------|------|
| 平台 | 微信小程序原生 | 无需 uni-app / Taro |
| 语言 | JavaScript (ES5) | 兼容低版本 iOS |
| UI | 原生组件 | 不引入第三方 UI 库 |
| 网络 | `wx.uploadFile` | multipart/form-data |

### 5.3 ✅ 已完成

- 拍照采集 / 从相册选照片
- 姓名输入 + 前端校验
- 上传到 Windows 后台
- 服务器配置（IP/端口，持久化）
- 隐私授权合规
- ES5 全链路兼容

---

## 六、开发完成度

```
✅ STM32：     底层通信 100%（DMA+RingBuffer + 帧协议 + 命令分发 + PC 联调）
✅ Windows 后台：100%（API + 管理界面 + Pi 同步 + 打包）
✅ 手机端：     100%（拍照 + 上传 + 配置）
⚠️ 树莓派：     ~10%（推理模块待开发）
⚠️ 全系统联调：   0%（待 Pi 端就绪后进行）

下一步：树莓派人脸推理模块开发
```

---

## 七、硬件连接拓扑

```
                    ┌─────────────────────┐
                    │    树莓派 4B 2G      │
                    │                     │
  USB摄像头 ──────► │ GPIO14(TXD) ──────► │
                    │ GPIO15(RXD) ◄────── │
                    │ GND ──────────────► │
                    └─────────────────────┘
                              │
                              │ UART 115200
                              ▼
                    ┌─────────────────────┐
                    │   STM32F103C8T6     │
                    │                     │
                    │ PA10(RX) ◄── Pi TX  │
                    │ PA9(TX)  ──► Pi RX  │
                    │ PB5 ────► LED1      │
                    │ PE5 ────► LED2      │
                    │ PB0 ────► 继电器     │
                    └─────────────────────┘

  手机(小程序) ─── WiFi/4G ───► Windows 后台 ─── SSH/SCP ───► 树莓派
```

---

> **文档维护：** 随项目进展持续更新。各模块详细实现见对应子目录下的 README。
